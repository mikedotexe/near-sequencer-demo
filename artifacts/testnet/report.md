# NEP-519 recipe book — NEAR testnet report

Generated by `scripts/demo.sh report` after running the four recipes on NEAR testnet. Each recipe section lists the runs, links to their transactions on https://testnet.nearblocks.io, and reports the observable block-level lifecycle.

## Invariants at a glance

**Invariants:** all three hold on the snapshotted runs.

- DAG-placement: **PASS** (40/40 trace events across 10 runs land in the expected yield-tx DAG)
- Budget (NEP-519 200-block timeout): **PASS** (2/2 runs inside the expected block window)
- Atomicity (Recipe 4): **PASS** (3/3 runs: each snapshotted Transfer matches recipient + amount + succeeded)

## Accounts

- `recipes` → `recipes.mike.testnet`
- `counter` → `recipes-counter.mike.testnet`
- `bob` (handoff recipient) → `bob.mike.testnet`

## Recipe 1 — Basic cross-tx yield + resume

- runs: 3 (resolved_ok: 3)
- yield→resume (blocks): median=4, min=4, max=4
- resume→callback (blocks): median=2, min=2, max=2
- DAG-placement invariant: **PASS** (9/9 trace events across 3 runs land in the expected tx DAG — the yield tx is the root of the receipt tree)

| run | yield tx | resume tx | outcome | yield→resume (blocks) | resume→callback (blocks) | link |
|-----|----------|-----------|---------|-----------------------|--------------------------|------|
| 1 | `58N5pcnEeRB8…` | `7Yg4hCSSxF7e…` | ok | 4 | 2 | [explorer](https://testnet.nearblocks.io/txns/7Yg4hCSSxF7egnCUjPfLu9c8b8vUynktxHe3ve3gjh37) |
| 2 | `8XQC48gj9zgB…` | `J4afSRSCvrHJ…` | ok | 4 | 2 | [explorer](https://testnet.nearblocks.io/txns/J4afSRSCvrHJRqKozjP8DQB8St5aSWPJ4qB7unXkCv6V) |
| 3 | `7kpBo1zmcMEY…` | `8URtjY1SS4EZ…` | ok | 4 | 2 | [explorer](https://testnet.nearblocks.io/txns/8URtjY1SS4EZY7Rz18qLcmNpxzUBY4ZQrJ7Q1LDn7UqZ) |

**Interpretation:** Each run yields in tx1, persisting the YieldId under `basic:<name>` in contract state. `Promise::new_yield` schedules the callback receipt at yield time — it lives in tx1's DAG, waiting for input. tx2 reads the YieldId and calls `yield_id.resume(...)`, which delivers the payload to that already-scheduled receipt; the callback executes and emits `recipe_resolved_ok`, still inside tx1's DAG. `resume→callback` is typically 1–2 blocks, reflecting how quickly the runtime picks up the waiting receipt after resume lands.

## Recipe 2 — Timeout: what happens when no one resumes

- runs: 1 (timeout_fired: 1)
- yield→callback (blocks): median=202, min=202, max=202
- expected: ~200 blocks (NEP-519 budget). Observed deltas close to 200 confirm the timeout fires as specified.
- DAG-placement invariant: **PASS** (2/2 trace events across 1 run land in the expected tx DAG — the yield tx is the root of the receipt tree)
- Budget invariant (NEP-519 200-block timeout): **PASS** (1/1 run inside [200, 205] blocks; observed=202)

| run | yield tx | timeout | yield→callback (blocks) | link |
|-----|----------|---------|-------------------------|------|
| 1 | `6z7USAzwMTpH…` | fired | 202 | [explorer](https://testnet.nearblocks.io/txns/6z7USAzwMTpHT49QwmkzfT1uktiQ3APLtpSJCcNHjXbp) |

**Interpretation:** The yield tx registers a yielded callback receipt at yield time and no resume is ever sent. After NEP-519's fixed 200-block budget elapses, the runtime delivers `PromiseError` to the already-scheduled callback, which runs with `#[callback_result] = Err(PromiseError)`. The contract's match arm records `recipe_resolved_err`. This confirms: **the callback always fires exactly once per yield, even in the absence of a resume**. (Observable evidence: the callback's receipt outcome is present in the yield tx's DAG, not a separate tx.)

## Recipe 3 — Chained: resume triggers a downstream call with callback

- runs: 3 (resolved_ok: 3)
- observed counter values: [1, 0, 1]
- yield→resume (blocks): median=4, min=4, max=5
- resume→dispatch (blocks): median=2, min=2, max=2
- dispatch→callback (blocks): median=2, min=2, max=2
- DAG-placement invariant: **PASS** (15/15 trace events across 3 runs land in the expected tx DAG — the yield tx is the root of the receipt tree)

| run | yield tx | resume tx | delta | observed counter | outcome | y→r (b) | r→d (b) | d→cb (b) | link |
|-----|----------|-----------|-------|------------------|---------|---------|---------|----------|------|
| 1 | `8nPnLRn3iteo…` | `CeFZJHwrnxr6…` | +1 | 1 | ok | 5 | 2 | 2 | [explorer](https://testnet.nearblocks.io/txns/CeFZJHwrnxr6bbA1wsMQ5gTG6G2BvTXietvxtzCy2uwf) |
| 2 | `7mDmoKSZTKU3…` | `7wn61dnpzRPr…` | -1 | 0 | ok | 4 | 2 | 2 | [explorer](https://testnet.nearblocks.io/txns/7wn61dnpzRPrXM8ZBdZJJ3BxGWJQPsiXokzeEhWb5qKq) |
| 3 | `Hf7qnFSJPgjU…` | `BPCKd5MyaFAH…` | +1 | 1 | ok | 4 | 2 | 2 | [explorer](https://testnet.nearblocks.io/txns/BPCKd5MyaFAHsyLBaSwd9pz1QgbZBYjpHV3Z2oUQZXHA) |

**Interpretation:** When resume lands, the pre-scheduled `on_chained_resumed` executes and dispatches `counter.increment()` (or decrement if delta < 0), chaining `.then(on_counter_observed)`. The counter callback reads the target's `#[callback_result] i8` and emits `recipe_callback_observed` with the new value. Only after that callback sees a truthful return does the recipe's own receipt resolve. Every downstream trace event — `recipe_dispatched`, `recipe_callback_observed`, `recipe_resolved_ok` — lives in the yield tx's DAG, not the resume tx's; resume is just the data delivery that unblocks the waiting receipt tree.

## Recipe 4 — Atomic handoff: value moves on resume or refunds on timeout

- runs: 3 (claim: 2, timeout: 1; settled_ok: 2)
- amount per handoff: 0.01 NEAR
- claim: yield→resume (blocks): median=7.5, min=7, max=8
- claim: yield→settle (blocks): median=9.5, min=9, max=10
- timeout: yield→settle (blocks): median=202, min=202, max=202 (NEP-519 budget = 200)
- DAG-placement invariant: **PASS** (14/14 trace events across 3 runs land in the expected tx DAG — the yield tx is the root of the receipt tree)
- Budget invariant (NEP-519 200-block timeout): **PASS** (1/1 run inside [200, 205] blocks; observed=202)
- Atomicity invariant: **PASS** (3/3 runs: each snapshotted Transfer matches expected recipient + amount + succeeded)

| run | mode | yield tx | resume tx | outcome | funds landed on | y→resume (b) | y→settle (b) | link |
|-----|------|----------|-----------|---------|-----------------|--------------|--------------|------|
| 1 | claim | `F67JHLG8PPZq…` | `9KncDTzhX6ZH…` | claimed | `bob.mike.testnet` | 7 | 9 | [explorer](https://testnet.nearblocks.io/txns/F67JHLG8PPZqY5dLmnkJfhbLkU7xxS4AkZm7nNdhciBS) |
| 2 | claim | `6K11oqRYzdoC…` | `2NqbUF2oMHFE…` | claimed | `bob.mike.testnet` | 8 | 10 | [explorer](https://testnet.nearblocks.io/txns/6K11oqRYzdoCvNLySaKeykFiAJiAB9bSVKgMvhmPRrdD) |
| 1 | timeout | `Hds1Rz9Untn3…` | — | refunded | `mike.testnet` | — | 202 | [explorer](https://testnet.nearblocks.io/txns/Hds1Rz9Untn3KfhUnWmyqzkgy7sFontUZLJcBVJqTkkF) |

**Interpretation:** Alice yields `recipe_handoff_yield(name, to=bob)` with 0.01 NEAR attached; the contract receives the deposit and schedules a callback parameterised with `(from, to, amount)`. On claim, a resume tx fires `recipe_handoff_resume(name)` and the waiting callback's Ok arm runs `Promise::new(to).transfer(amount)` — the funds flow to the nominated recipient, wherever the resumer came from. On timeout, no resume is ever sent; after 200 blocks the runtime delivers `PromiseError` to the same callback, whose Err arm runs `Promise::new(from).transfer(amount)` and refunds Alice. **The single receipt scheduled at yield time carries both endings.** No escrow table, no refund method, no polling — the yield/resume primitive alone moves value atomically, with the 200-block budget as the built-in safety valve.

## How to reproduce

```sh
scripts/demo.sh build
scripts/demo.sh deploy
scripts/demo.sh run basic --repeat 3
scripts/demo.sh run timeout --repeat 1   # each run waits ~4 min
scripts/demo.sh run chained --repeat 3
scripts/demo.sh run handoff --mode claim --repeat 2
scripts/demo.sh run handoff --mode timeout --repeat 1   # each waits ~4 min
scripts/demo.sh audit basic && scripts/demo.sh audit timeout \
    && scripts/demo.sh audit chained && scripts/demo.sh audit handoff
scripts/demo.sh aggregate && scripts/demo.sh report
```

Every tx hash above links to https://testnet.nearblocks.io, so each lifecycle claim is independently verifiable from the public chain. Sibling `run-NN.onchain.json` files snapshot the same data locally (full receipt DAGs, blocks, chunks) for offline reanalysis.
